import React from 'react';
import { Link, Outlet } from 'react-router-dom';

const Order = () => {
    return (
        <div>
            <h1>Order</h1>
        </div>
    );
};

export default Order;